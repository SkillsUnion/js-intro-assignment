// What's going on here? What "rules," if any, can we guess from testing these examples?

test("Concatenate 1", () => {
  // Fix here!
  expect("hamburger" + "s").toBe(null);
});

test("Concatenate 2", () => {
  // Fix here!
  expect("hamburger" - "s").toBe(null);
});

test("Concatenate 3", () => {
  // Fix here!
  expect("4" + "2").toBe(null);
});

test("Concatenate 4", () => {
  // Fix here!
  expect("4" - "2").toBe(null);
});

test("Concatenate 5", () => {
  // Fix here!
  expect("johnny" + 5).toBe(null);
});

test("Concatenate 6", () => {
  // Fix here!
  expect("johnny" - 5).toBe(null);
});

test("Concatenate 7", () => {
  // Fix here!
  expect(99 * "baloons").toBe(null);
});
